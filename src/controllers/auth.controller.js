const userModel = require("../models/user.model");

/**
 * @name registerUserController
 * @description register new user, expect username,email and password in the request body
 * @access Public
 */
async function registerUserController(reg, res) {}

module.exports = {
  registerUserController,
};
